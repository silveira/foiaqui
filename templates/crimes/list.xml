<crimes>
{% if not incident_list %}ERROR{% endif %}
{% for incident in incident_list %}
<incident lat="{{incident.lat}}" lng="{{incident.lon}}" desc="{{incident.desc|linebreaks|escape}}" date="{{incident.when}}"/>
{% endfor %}
</crimes>
